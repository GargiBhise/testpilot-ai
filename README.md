# testpilot-ai
AI agent that analyzes GitHub repositories, generates Playwright tests, executes them, and stores test run history.

## Safety Scope

For the MVP, TestPilot AI will only be tested on repositories I own or repositories where I have explicit permission.

The tool will analyze source code structure and generate Playwright tests, but it will not run tests against third-party production websites without authorization.

